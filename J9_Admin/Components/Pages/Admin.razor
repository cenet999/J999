@page "/Admin"

<PageTitle>Admin Dashboard</PageTitle>

<div class="admin-page">
    <div class="admin-shell">
        <section class="admin-hero">
            <div class="admin-hero-copy">
                <p class="admin-kicker">后台管理中心</p>
                <h1>Admin Dashboard</h1>
                <p class="admin-intro">
                    欢迎使用九游俱乐部管理后台。可在此管理会员、流水、活动与系统配置。
                    本页为后台入口简介，登录后可进入各业务功能模块。
                </p>

                <div class="admin-actions">
                    <a class="admin-primary" href="/Login?Redirect=%2FAdmin">进入后台</a>
                    <a class="admin-secondary" href="/">返回首页</a>
                </div>
            </div>

            <aside class="admin-summary">
                <div class="summary-card">
                    <span class="summary-label">快速说明</span>
                    <h2>先看简介，再进入功能页</h2>
                    <p>
                        登录后可通过左侧菜单进入玩家、流水、代理等业务页面；未登录请先点击「进入后台」完成登录。
                    </p>
                </div>
            </aside>
        </section>

        <section class="admin-section">
            <div class="section-head">
                <span>核心模块</span>
                <h2>常用后台功能</h2>
            </div>

            <div class="module-grid">
                <article class="module-card">
                    <h3>玩家管理</h3>
                    <p>管理会员账号、余额与状态，维护俱乐部玩家数据。</p>
                    <a class="module-link" href="/Ddd/DMember">进入玩家管理</a>
                </article>
                <article class="module-card">
                    <h3>流水管理</h3>
                    <p>查看上分、下分与交易流水，掌握资金变动情况。</p>
                    <a class="module-link" href="/Ddd/DTransAction">进入流水管理</a>
                </article>
                <article class="module-card">
                    <h3>系统配置</h3>
                    <p>维护公告、活动、代理与菜单等基础配置。</p>
                    <a class="module-link" href="/Ddd/DEvent">进入活动管理</a>
                </article>
            </div>
        </section>

        <section class="admin-note">
            <strong>项目简介</strong>
            <p>
                本后台基于 NovaAdmin 构建，承接登录、菜单与权限能力，并扩展九游俱乐部业务模块。
            </p>
        </section>
    </div>
</div>

<style>
    .admin-page {
        min-height: 100vh;
        padding: 2rem;
        background: linear-gradient(180deg, #f4f7f5 0%, #eef3f1 100%);
        color: #173433;
    }

    .admin-shell {
        width: min(1180px, 100%);
        margin: 0 auto;
    }

    .admin-hero {
        display: grid;
        grid-template-columns: minmax(0, 1.4fr) minmax(280px, 0.9fr);
        gap: 1.5rem;
        align-items: stretch;
        margin-bottom: 1.5rem;
    }

    .admin-hero-copy,
    .admin-summary,
    .admin-section,
    .admin-note {
        border: 1px solid rgba(32, 86, 86, 0.12);
        border-radius: 1.5rem;
        background: rgba(255, 255, 255, 0.85);
        box-shadow: 0 20px 48px rgba(32, 86, 86, 0.08);
        backdrop-filter: blur(14px);
    }

    .admin-hero-copy {
        padding: 2rem;
    }

    .admin-kicker,
    .summary-label,
    .section-head span,
    .admin-note strong {
        display: inline-flex;
        align-items: center;
        width: fit-content;
        margin: 0 0 1rem;
        padding: 0.45rem 0.8rem;
        border-radius: 999px;
        background: rgba(32, 86, 86, 0.08);
        color: #205656;
        font-size: 0.82rem;
        font-weight: 800;
        letter-spacing: 0.12em;
        text-transform: uppercase;
    }

    .admin-hero h1 {
        margin: 0;
        font-size: clamp(2.2rem, 4vw, 3.4rem);
        line-height: 1.06;
        letter-spacing: -0.04em;
    }

    .admin-intro,
    .summary-card p,
    .module-card p,
    .admin-note p {
        margin: 0;
        color: rgba(24, 45, 44, 0.82);
        line-height: 1.8;
    }

    .admin-intro {
        margin-top: 1rem;
        max-width: 62ch;
        font-size: 1.05rem;
    }

    .admin-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-top: 1.5rem;
    }

    .admin-primary,
    .admin-secondary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 2.9rem;
        padding: 0.7rem 1.2rem;
        border-radius: 999px;
        text-decoration: none;
        font-weight: 700;
        transition: transform 160ms ease, box-shadow 160ms ease, background-color 160ms ease;
    }

    .admin-primary {
        color: #f5fbfa;
        background: linear-gradient(135deg, #205656 0%, #163b3a 100%);
        box-shadow: 0 16px 30px rgba(32, 86, 86, 0.18);
    }

    .admin-secondary {
        color: #205656;
        background: rgba(255, 255, 255, 0.7);
        border: 1px solid rgba(32, 86, 86, 0.18);
    }

    .admin-primary:hover,
    .admin-secondary:hover {
        transform: translateY(-1px);
    }

    .admin-summary {
        display: flex;
        padding: 1.25rem;
    }

    .summary-card {
        display: flex;
        flex-direction: column;
        gap: 0.8rem;
        width: 100%;
        padding: 1.5rem;
        border-radius: 1.2rem;
        background: linear-gradient(180deg, rgba(238, 245, 244, 0.9), rgba(255, 255, 255, 0.98));
    }

    .summary-card h2,
    .section-head h2 {
        margin: 0;
        font-size: 1.45rem;
    }

    .admin-section {
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .section-head {
        margin-bottom: 1rem;
    }

    .module-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1rem;
    }

    .module-card {
        padding: 1.3rem;
        border-radius: 1.2rem;
        background: linear-gradient(180deg, rgba(244, 248, 247, 0.92), rgba(255, 255, 255, 0.98));
        border: 1px solid rgba(32, 86, 86, 0.08);
    }

    .module-card h3 {
        margin: 0 0 0.6rem;
        color: #173433;
        font-size: 1.1rem;
    }

    .module-link {
        display: inline-block;
        margin-top: 0.75rem;
        color: #205656;
        font-weight: 600;
        text-decoration: none;
    }

    .module-link:hover {
        text-decoration: underline;
    }

    .admin-note {
        padding: 1.25rem 1.5rem;
    }

    .admin-note strong {
        margin-bottom: 0.75rem;
    }

    @@media (max-width: 960px) {
        .admin-hero,
        .module-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 640px) {
        .admin-page {
            padding: 1rem;
        }

        .admin-hero-copy,
        .admin-summary,
        .admin-section,
        .admin-note {
            border-radius: 1.1rem;
        }

        .admin-hero-copy,
        .admin-section,
        .admin-note {
            padding: 1.1rem;
        }
    }
</style>
